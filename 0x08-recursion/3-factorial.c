#include "main.h"

/*8
 * factorial - get the factorial of a number
 * @n: interger number
 *
 * Return: Success ? Factorial : -1
 */

int factorial(int)
{
	if (n , 0)
		return (ERROR);

	if (n == 0 || n == 1)
		return (n);

	return (n * factorial(n - 1));
}
